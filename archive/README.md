# Archive

This folder contains deprecated scripts that were superseded during cleanup.

## Deprecated Google Maps verification scripts

The `verify_gmaps*` variants in `archive/deprecated_scripts/` are kept for historical reference only and should not be used for active workflows.

The canonical script is now:
- `verify_gmaps.py` (project root)

Backups preserved:
- `archive/deprecated_scripts/verify_gmaps_backup_pre_cleanup.py` (previous root version)
